version: 2
jobs:
  build:
    docker:
      - image: circleci/rust:latest
    steps:
      - checkout
      - restore_cache:
          key: project-cache
      - run: cargo build
      - save_cache:
          key: project-cache
          paths:
            - "~/.cargo"
            - "./target"